# Problem / Solutions Log

Every fix logged here as it happens.

| # | Date | Problem | Solution |
|---|------|---------|----------|
| 1 | 2026-08-11 | "0 jailbreak successes" was NOT Gemini refusing — GLM agent calls were silently failing. Root causes: (a) `glm-4-flash` does not exist on the user's BigModel account (available: glm-4.5 → glm-5.2); (b) GLM account has **insufficient balance** (error 1113 "余额不足,请充值"); (c) `gemini-2.0-flash` is deprecated/removed — the key returned quota `limit: 0` for it. | Changed defaults in `core/config.py`: GLM_MODEL=`glm-5-turbo`, GLM_ATTACKER_MODEL=`glm-5.2`, GEMINI_MODEL=`gemini-2.5-flash` (verified working with the key). GLM still blocked until user tops up credits. |
| 2 | 2026-08-11 | GLM attacker/judge/mutator had no working model on the user's account (balance issue). | User requested `glm-4.7-flash` — it works and is **free-tier** (no balance needed). Set GLM_MODEL / GLM_ATTACKER_MODEL / GLM_JUDGE_MODEL to `glm-4.7-flash`. Added `thinking: {"type":"disabled"}` to GLM payload (reasoning model, otherwise tokens burn on reasoning and `content` can come back empty) + fallback to `reasoning_content`. |
| 3 | 2026-08-11 | Real end-to-end test after fixes: **3/7 attacks succeeded** against `gemini-2.5-flash` (PROMPT_INJECTION=MEDIUM, TOKEN_SMUGGLING=HIGH, PERSONA_SWITCH=HIGH), success_rate 42.9%, risk LOW 24.4. | Stronger attacker (BYPASS_TECHNIQUES catalog + failure feedback → targeted mutation) + working models. Pipeline now genuinely detects jailbreaks. |
| 4 | 2026-08-11 | Supabase MCP is **read-only** (`Cannot apply migration in read-only mode`, and DDL via `execute_sql` fails with `cannot execute CREATE TABLE in a read-only transaction`), so Phase 2 schema could not be applied through MCP tools. | Used the Supabase **Management API** `POST /v1/projects/{ref}/database/query` with the CLI access token (`sbp_*`, read from Windows Credential Manager via P/Invoke CredRead) to run the `runs`/`attacks` DDL. Applied successfully. |
| 5 | 2026-08-11 | Phase 2 DB + SSE verification — in-memory store replaced with Supabase async client (`create_async_client`). SSE rewritten to **poll** Supabase with an offset instead of pub/sub so manual inserts + reconnects stream too. | Smoke-tested: create run → log 2 attacks (counters 2/1) → complete (`completed_at` set) → offset reads → list ordering; live SSE returned the attack JSON event then `{"type":"done"}`. All passed, test data cleaned up. Added `ruff` for linting. |
| 6 | 2026-08-11 | Phase 3 frontend — Next.js 16 scaffolded (`next/font/google` + Tailwind v4 + React 19). React-hooks ESLint rules (`react-hooks/refs`, `react-hooks/set-state-in-effect`) rejected idiomatic data-fetching patterns. | Moved ref writes (`onStatsRef.current = …`) into a `useEffect`; rewrote page `load()` callbacks as promise chains so `setState` only runs after `await` (no synchronous setState in effects). `npm run lint` + `npm run build` now pass. |
| 7 | 2026-08-11 | Phase 3 runtime smoke test — backend on :8000, `next start` on :3000. | Verified `GET /api/config` 200, `/api/runs` 200, and all 4 frontend routes (`/`, `/runs`, `/runs/[id]`, `/reports/[id]`) return 200. Servers stopped after test. |
| 8 | 2026-08-11 | Phase 4 backend lint — `ruff check` failed on the whole backend (long prompt templates E501, unused imports F401, import sorting I001, unused loop var B007, blind-except BLE001). | Added `backend/pyproject.toml` ruff config (`select E,F,I,W,UP,B`, `ignore BLE001`, `line-length 120`, per-file E501 ignore for `config.py`/`targets/`/`agents/` template files). Ran `ruff --fix --unsafe-fixes`; `ruff check .` now passes. mypy not configured → typecheck = `compileall` (passes). |
| 9 | 2026-08-11 | Phase 4 E2E — live run via API. Real run: `gemini-2.5-flash` target, ROLEPLAY_BYPASS + TOKEN_SMUGGLING. | Completed: 5 attacks, 2 jailbreaks (40% success), risk LOW 20.9, report JSON valid (summary/severity/category/top-vulns/recs). Live SSE streamed attack events. Found a race: `done` could fire while a trailing attack row was still unread. Fixed SSE to do a final trailing fetch after status is terminal before emitting `done` (`main.py` `/api/runs/{id}/stream`). |
| 10 | 2026-08-11 | Report generator verification — sample dataset (5 results, 3 successes incl. CRITICAL/HIGH/MEDIUM). | `generate_report()` matches spec: total/success/rate, severity breakdown, category stats, top-3 by confidence, 3 recommendations; `calculate_risk_score` = 33.6 (correct weighted formula), `get_risk_level` thresholds LOW/MEDIUM/HIGH/NEGLIGIBLE verified; empty-run edge case returns zeros. |
